import { PostWithExtra } from "@/lib/alltypes";
import Image from "next/image";
import React from "react";

const ShowComments = ({ posts }: { posts: PostWithExtra }) => {
    
  return (
    <div>
      <div></div>
    </div>
  );
};

export default ShowComments;
